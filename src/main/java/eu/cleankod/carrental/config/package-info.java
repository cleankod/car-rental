/**
 * Spring {@code @Configuration} and {@code @ConfigurationProperties} classes wiring the application's
 * beans — kept separate from domain/application/adapter so those layers stay framework-agnostic.
 */
package eu.cleankod.carrental.config;
