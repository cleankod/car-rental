/**
 * Thread-safe in-memory implementation of the outbound persistence port — no external database,
 * per this assignment's decision to simulate persistence rather than stand up a real one.
 */
package eu.cleankod.carrental.adapter.out.persistence;
