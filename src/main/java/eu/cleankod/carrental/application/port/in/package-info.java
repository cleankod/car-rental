/**
 * Inbound port(s): the use-case interface(s) the application exposes to callers — tests directly,
 * and the REST adapter if that optional stage is built.
 */
package eu.cleankod.carrental.application.port.in;
