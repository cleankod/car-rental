/**
 * Application services orchestrating the reservation use case against the domain and the ports
 * below — depends on domain classes and port interfaces only, never on adapter technologies directly.
 */
package eu.cleankod.carrental.application;
